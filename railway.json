{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo '=START=' && python manage.py migrate --noinput && echo '=MIGRATE OK=' && python -m gunicorn config.wsgi:application --bind 0.0.0.0:8080 --workers 1 --timeout 120",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
